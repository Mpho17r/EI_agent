# EI_agent
# Mpho17r AI Project Tracker

## Overview
Internal system for managing projects, sprints, and tasks.

## Workflow
- Feature branches only
- Pull Requests required
- CI must pass before merge

## Documentation
See /docs folder
git add README.md
git commit -m "Test: checking CI pipeline"
git push origin test/ci-test
